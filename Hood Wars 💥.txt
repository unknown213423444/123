local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "BEST SCRIPT",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "Hood Wars 💥",
   LoadingSubtitle = "by Saqxzz",
   Theme = "Amber Glow", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = false,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = false, -- Set this to true to use our key system
   KeySettings = {
      Title = "Untitled",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"Hello"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

local MainTab = Window:CreateTab("Home🏠", nil) -- Title, Image
local MainSection = MainTab:CreateSection("WEAPONS🔫")

local Button = MainTab:CreateButton({
   Name = "SPAWN GLOCK SWITCH",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("Glock19DrumSwitch") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN AK47",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("AK47") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN MP5",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("MP5") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN MICRO ARP",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("Micro ARP") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN GLOCK 23",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("Glock 23") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN DRACO",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("Draco") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN COMPACT 45",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("Compact 45") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN UZI",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("Uzi") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN TAN ARP DRUM",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("TanARPDrum") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN BLACKOUT",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("Blackout") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN FIVESEVEN EXT",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("FiveSeven Extended") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN MH SHOTGUN",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("MHShotgun") 
end
   end,
})

local Button = MainTab:CreateButton({
   Name = "SPAWN MH13",
   Callback = function()
   local addGunsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddGuns")

if addGunsEvent then
    addGunsEvent:FireServer("MH13") 
end
   end,
})

local MiscTab = Window:CreateTab("MISC⚙", nil) -- Title, Image
local Section = MiscTab:CreateSection("MONEY💸")

local Button = MiscTab:CreateButton({
   Name = "ADD MAX MONEY",
   Callback = function()
   local addCoinsEvent = game:GetService("ReplicatedStorage"):FindFirstChild("AddCoins")

if addCoinsEvent then
    addCoinsEvent:FireServer(100000000)
end
   end,
})